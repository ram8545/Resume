import React from "react";

const Objective = ({ data }) => {
  return (
    <section className="objective">
      <h3>Objective</h3>
      <p>{data}</p>
    </section>
  );
};

export default Objective;
